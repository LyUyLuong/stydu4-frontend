# Used by Vite at `npm run build` time (auto-loaded for production builds).
# In production, FE and BE share the same domain (nginx routes /api/v1/* to Spring Boot),
# so a relative path is correct — no host needed.
VITE_API_BASE_URL=/api/v1
